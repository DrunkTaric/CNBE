module.exports = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        text: "var(--text)",
        loading: "var(--load)",
        background: "var(--bg)",
        container: "var(--bg-sec)",
        reactive: "var(--reactive)"
      }
    }
  }
}
